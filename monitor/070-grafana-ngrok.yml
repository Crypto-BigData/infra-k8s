apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-ngrok-env
  namespace: monitor
data:
  NGROK_TARGET: "http://grafana.monitor.svc.cluster.local:3000"
  NGROK_DOMAIN: ""
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: grafana-ngrok
  namespace: monitor
spec:
  replicas: 1
  selector:
    matchLabels:
      app: grafana-ngrok
  template:
    metadata:
      labels:
        app: grafana-ngrok
    spec:
      containers:
        - name: ngrok
          image: ngrok/ngrok:latest
          imagePullPolicy: IfNotPresent
          command: ["/bin/sh", "-c"]
          args:
            - |
              if [ -n "${NGROK_DOMAIN}" ]; then
                ngrok http --log=stdout --log-format=logfmt --domain="${NGROK_DOMAIN}" "${NGROK_TARGET}"
              else
                ngrok http --log=stdout --log-format=logfmt "${NGROK_TARGET}"
              fi
          env:
            - name: NGROK_AUTHTOKEN
              valueFrom:
                secretKeyRef:
                  name: ngrok-auth-grafana
                  key: NGROK_AUTHTOKEN
            - name: NGROK_TARGET
              valueFrom:
                configMapKeyRef:
                  name: grafana-ngrok-env
                  key: NGROK_TARGET
            - name: NGROK_DOMAIN
              valueFrom:
                configMapKeyRef:
                  name: grafana-ngrok-env
                  key: NGROK_DOMAIN
          ports:
            - containerPort: 4040
              name: inspector
          resources:
            requests:
              cpu: 50m
              memory: 64M
---
apiVersion: v1
kind: Service
metadata:
  name: grafana-ngrok
  namespace: monitor
spec:
  selector:
    app: grafana-ngrok
  ports:
    - name: inspector
      port: 4040
      targetPort: inspector

