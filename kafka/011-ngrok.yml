apiVersion: v1
kind: ConfigMap
metadata:
  name: kafka-ui-ngrok-env
  namespace: kafka
data:
  NGROK_TARGET: "http://kafka-ui.kafka.svc.cluster.local:8080"
  NGROK_DOMAIN: ""
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kafka-ui-ngrok
  namespace: kafka
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kafka-ui-ngrok
  template:
    metadata:
      labels:
        app: kafka-ui-ngrok
    spec:
      containers:
        - name: ngrok
          image: ngrok/ngrok:latest
          imagePullPolicy: IfNotPresent
          command: ["/bin/sh", "-c"]
          args:
            - |
              if [ -n "${NGROK_DOMAIN}" ]; then
                ngrok http --log=stdout --log-format=logfmt --domain="${NGROK_DOMAIN}" "${NGROK_TARGET}"
              else
                ngrok http --log=stdout --log-format=logfmt "${NGROK_TARGET}"
              fi
          env:
            - name: NGROK_AUTHTOKEN
              valueFrom:
                secretKeyRef:
                  name: ngrok-auth-kafka
                  key: NGROK_AUTHTOKEN
            - name: NGROK_TARGET
              valueFrom:
                configMapKeyRef:
                  name: kafka-ui-ngrok-env
                  key: NGROK_TARGET
            - name: NGROK_DOMAIN
              valueFrom:
                configMapKeyRef:
                  name: kafka-ui-ngrok-env
                  key: NGROK_DOMAIN
          ports:
            - containerPort: 4040
              name: inspector
          resources:
            requests:
              cpu: 50m
              memory: 64M
