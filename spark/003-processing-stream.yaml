apiVersion: sparkoperator.k8s.io/v1beta2
kind: SparkApplication
metadata:
  name: processing-stream
  namespace: app
spec:
  type: Python
  pythonVersion: "3"
  mode: cluster
  image: lucaswux/processing-stream:dev-20260617-223755
  mainApplicationFile: local:///opt/app/src/processing_stream/kafka_raw_to_minio.py
  sparkVersion: "3.5.8"
  restartPolicy:
    type: Always
  deps:
    packages:
      - org.apache.spark:spark-sql-kafka-0-10_2.12:3.5.8
      - org.apache.hadoop:hadoop-aws:3.3.4
  sparkConf:
    spark.jars.ivy: /tmp/.ivy2
    spark.sql.session.timeZone: UTC
    spark.sql.streaming.stopGracefullyOnShutdown: "true"
  driver:
    cores: 1
    coreRequest: "500m"
    memory: 512m
    serviceAccount: processing-stream
    labels:
      app.kubernetes.io/name: processing-stream
    envFrom:
      - configMapRef:
          name: processing-stream-config
  executor:
    instances: 1
    cores: 1
    coreRequest: "500m"
    memory: 512m
    labels:
      app.kubernetes.io/name: processing-stream
    envFrom:
      - configMapRef:
          name: processing-stream-config
